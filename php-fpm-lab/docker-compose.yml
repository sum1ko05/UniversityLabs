version: "3.9"

services:
  nginx:
    image: nginx:1.27-alpine
    container_name: lab2_nginx
    ports:
      - "8080:80"
    volumes:
      - ./code:/usr/share/nginx/html
    depends_on:
      - php
  php:
    image: php:8.2-fpm
    container_name: lab2_php
    volumes:
      - ./www:/var/www/html
    expose:
      - "9000"