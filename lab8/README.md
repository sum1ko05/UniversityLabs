# Лабораторная работа №8: Тестирование PHP-приложения с использованием PHPUnit и Guzzle

## 👩‍💻 Автор
ФИО: Хуснутдинов Роман Сергеевич

Группа: 2ИП-1

---

## 📌 Описание задания

Научиться:

1. Устанавливать и использовать PHPUnit

2. Писать unit-тесты для классов

3. Использовать mock-объекты

4. Тестировать HTTP-запросы через Guzzle

5. Работать с переменными окружения (.env)

6. Изолировать тестовую среду


Результат доступен по адресу [http://localhost:8080](http://localhost:8080).

Adminer доступен по адресу [http://localhost:8082](http://localhost:8082).

---

## ⚙️ Как запустить проект

1. Клонировать ветку репозитория:
   ```bash
   git clone -b web https://github.com/sum1ko05/UniversityLabs.git
   cd UniversityLabs/php-composer-lab
   ```
2. Запустить контейнеры:
   ```bash
   docker compose up -d --build
   ```
3. Открыть в браузере главную страницу:
   ```http://localhost:8080```
4. (Опционально) Запустить тесты:
   ```bash
   vendor/bin/phpunit tests
   ```

📂 Содержимое проекта

```docker-compose.yml``` — описание сервисов, необходимо подредактировать перед запуском

```.env.example``` — образец .env файла проекта. При написании .env файла обязательно заменить секреты на свои! 

```init.sql``` — стартовый SQL запрос, создающий рабочую таблицу

```database/``` — каталог с базой данных. При отсутствии в проекте необходимо его создать

```public/``` — каталог со всеми файлами для демонстрации

```tests/``` — каталог с тестами

✅ Результат

PHPUnit установлен и используется. Unit-тесты написаны для класса Order с mock-объектами. HTTP-запросы протестированы. Переменные окружения внедрены через .env файл.